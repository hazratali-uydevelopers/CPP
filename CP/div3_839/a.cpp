#include <bits/stdc++.h>
using namespace std;

int main () {
    int n, x, y;
    char c;
    cin >> n;
    
    while (n--) {
        cin >> x >> c >> y;
        cout << x + y << endl;
    }

    return 0;
}
